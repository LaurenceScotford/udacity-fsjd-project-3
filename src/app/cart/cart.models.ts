export interface CartItem {
    productId: string;
    quantity: number;
}

export interface CartItem {
    productId: string;
    quantity: number;
}

export interface CartState {
    cartItems: CartItem[];
}